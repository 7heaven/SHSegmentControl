language: android
jdk:
  - oraclejdk8

android:
  components:
	- tools
    - build-tools-25.0.0
	- platform-tools
    - android-23

notifications:
  email: true

cache: false

sudo: false

install:

  - chmod +x ./gradlew; ls -l gradlew; ./gradlew wrapper -v

script:
  - ./gradlew clean assembleDebug

